variant: flatcar
version: 1.1.0
passwd:
  users:
    - name: core
      ssh_authorized_keys: ${jsonencode(ssh_keys)}
storage:
  files:
    - path: /etc/motd
      filesystem: root
      mode: 0644
      contents:
        inline: |
          ${message}