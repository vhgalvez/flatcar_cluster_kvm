variant: flatcar
version: 1.1.0
passwd:
  users:
    - name: core
      ssh_authorized_keys:
${indent(8, join("\n", formatlist("- \"%s\"", ssh_keys)))}
storage:
  files:
    - path: "/etc/motd"
      mode: 0644
      contents:
        inline: |
          ${message}