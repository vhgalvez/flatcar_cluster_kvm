variant: flatcar
version: 1.0.0
passwd:
  users:
    - name: core
      ssh_authorized_keys:
        - ${jsonencode(ssh_keys)}
storage:
  files:
    - filesystem: "root"
      path: "/etc/motd"
      mode: 0644
      contents:
        inline: |
          ${message}
